import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  animatedView: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
  },
  wrapper: {
    width: '100%',
    height: '100%',
  },
});
